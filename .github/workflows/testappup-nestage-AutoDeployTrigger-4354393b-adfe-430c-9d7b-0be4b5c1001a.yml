name: Trigger auto deployment for containerapp testappup-nestage

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/testappup-nestage-AutoDeployTrigger-4354393b-adfe-430c-9d7b-0be4b5c1001a.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to container registry
        uses: docker/login-action@v1
        with:
          registry: catestappupnacr.azurecr.io
          username: ${{ secrets.TESTAPPUPNESTAGE_REGISTRY_USERNAME }}
          password: ${{ secrets.TESTAPPUPNESTAGE_REGISTRY_PASSWORD }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: catestappupnacr.azurecr.io/testappup-nestage:${{ github.sha }}
          file: ./Dockerfile
          context: ./


  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.TESTAPPUPNESTAGE_AZURE_CREDENTIALS }}


      - name: Deploy to containerapp
        uses: azure/CLI@v1
        with:
          inlineScript: |
            echo "Installing containerapp extension"
            az config set extension.use_dynamic_install=yes_without_prompt
            echo "Starting Deploying"
            az containerapp registry set -n testappup-nestage -g sisirap-newrg22 --server catestappupnacr.azurecr.io --username  ${{ secrets.TESTAPPUPNESTAGE_REGISTRY_USERNAME }} --password ${{ secrets.TESTAPPUPNESTAGE_REGISTRY_PASSWORD }}
            az containerapp update -n testappup-nestage -g sisirap-newrg22 --image catestappupnacr.azurecr.io/testappup-nestage:${{ github.sha }}
